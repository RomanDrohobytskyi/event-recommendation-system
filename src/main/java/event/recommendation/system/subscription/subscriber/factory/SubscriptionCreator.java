package event.recommendation.system.subscription.subscriber.factory;

import event.recommendation.system.entities.User;
import event.recommendation.system.enums.SubscriptionType;

public class SubscriptionCreator {

    public void createSubscription(SubscriptionType subscriptionType, User user) {

    }
}
